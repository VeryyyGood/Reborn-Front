import React from "react";
import {View, Text,Button,TouchableOpacity} from "react-native";

const ItemShareScreen = ({navigation: { navigate }} ) => (
    <TouchableOpacity >
        <Text>물품 나눔</Text>
    </TouchableOpacity>
);

export default ItemShareScreen;