import React from "react";
import {View, Text,Button,TouchableOpacity} from "react-native";

const ChatShareScreen = ({navigation: { navigate }} ) => (
    <TouchableOpacity >
        <Text>담소 나눔</Text>
    </TouchableOpacity>
);

export default ChatShareScreen;