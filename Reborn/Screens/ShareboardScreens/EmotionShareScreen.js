import React from "react";
import {View, Text,Button,TouchableOpacity} from "react-native";

const EmotionShareScreen = ({navigation: { navigate }} ) => (
    <TouchableOpacity >
        <Text>감정 나눔 시키야</Text>
    </TouchableOpacity>
);

export default EmotionShareScreen;